{
  "name": "to-ndjson",
  "version": "1.0.0",
  "description": "Convert a JSON array of objects to line-delimited JSON without parsing the entire array into memory",
  "keywords": [
    "json",
    "ndjson",
    "ldjson"
  ],
  "main": "index.js",
  "files": [
    "*.js"
  ],
  "scripts": {
    "pretest": "prettier --write '*.js'",
    "test": "tape test.js"
  },
  "author": "Dan MacTough <danmactough@gmail.com> (http://mact.me)",
  "license": "MIT",
  "dependencies": {
    "jsonparse": "^1.3.1"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "tape": "^5.0.1"
  },
  "prettier": {
    "singleQuote": true
  }
}
